/**
 * TAD §11.6 — the date/comparison pickers land here in Phase 2 (item 2.4). Phase 0
 * only needs the shell to exist and mount once, alongside the sidebar.
 */
export function TopBar() {
  return (
    <header>
      <h1>TechnoRUCS</h1>
      <span>CMO Dashboard</span>
    </header>
  )
}
